Auto Text Slider
A simple JavaScript example that automatically cycles through a list of items.
What it does
Stores items in an array
Displays each item inside a box
Changes the displayed text every 2 seconds using setInterval
Loops back to the first item after the last one
Purpose Practice arrays, DOM manipulation, and timers (setInterval) in JavaScript.
